{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://vesper-backend-production.up.railway.app/api/:path*"
    }
  ],
  "buildCommand": "cd frontend && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "frontend/dist"
}
