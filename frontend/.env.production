# Production environment — used by Vercel builds and `npm run build`
# Points directly at Railway backend (bypasses Vercel proxy for reliability)
VITE_API_URL=https://vesper-backend-production-b486.up.railway.app
